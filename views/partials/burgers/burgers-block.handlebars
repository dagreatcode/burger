<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}BURGER TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
